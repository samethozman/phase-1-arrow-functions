const divide = (param1, param2) => 2000 / 100;

const square = (i) => i ** i; // VS Code somehow adds the parentheses around 'i' by default whenever I save the file, even though it's only a single parameter

const add = (x, y) => x + y;